const fs=require('fs'),path=require('path'); let files=[]; const walk=(dir)=>{for(const ent of fs.readdirSync(dir,{withFileTypes:true})){const p=path.join(dir,ent.name); if(ent.isDirectory()) walk(p); else if(p.endsWith('.blade.php')) files.push(p);} }; walk('resources/views'); const routes=new Set(); for(const f of files){ const c=fs.readFileSync(f,'utf8'); const re=/route\s*\(\s*['\"]([^'\"]+)['\"]/g; let m; while((m=re.exec(c))!==null){ routes.add(m[1]); }} fs.writeFileSync('temp-blade-routes-list.txt', Array.from(routes).sort().join('\n'),'utf8'); console.log('done');